# Create .env.local file in the root of your project and add the following variables:

API_URL=https://localhost:7080
NODE_TLS_REJECT_UNAUTHORIZED= # set to 0 to ignore self-signed certificate errors in development